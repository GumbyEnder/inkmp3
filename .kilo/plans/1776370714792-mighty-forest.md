# Plan: Enhance Search Results Display

## Completed

### 1. Add Ctrl+M for More Results

- Changed keybinding from `]` to `ctrl+m`
- Changed `[` to `ctrl+l`
- Updated hint text in SearchLayout

### 2. Fix Tab Key for Search Type Filter

- Added `bypassBlock: true` to Tab keybinding in SearchBar
- Tab now works to switch search types even when keyboard is blocked

### 3. Enhance Search Results Display

- Added duration column (always shown for songs, formatted as mm:ss)
- Added artist name column (shown for songs)
- Added album name column (shown for songs when available)
- Added color highlighting:
  - Type badges: primary for songs, accent for artists, secondary for albums
  - Artist: accent color
  - Album: dim color
  - Duration: secondary color

YouTube tracks still work:

```bash
ymc play "dQw4w9WgXcQ"  # or any video ID
ymc search "song name"
```
